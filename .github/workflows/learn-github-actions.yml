
name: Entropy CI/CD Pipeline
      
# on:
#   release:
#     types: [released]
#     branches:
#       - 'cloned/develop'

on:
  push:
    branches:
      - 'CI-CD'  

jobs:
  Deploy:
    runs-on: ubuntu-20.04
#     needs: Build
    steps:
      - name: Run ./Deploy.sh file  📁
        uses: appleboy/ssh-action@v0.1.4
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{secrets.SSH_USER}}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            cd entropy 
            git remote set-url origin https://jaydip-9548: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@github.com/hplustree/entropy.git
            git fetch 
